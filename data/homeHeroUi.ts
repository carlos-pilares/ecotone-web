/**
 * Home hero UI constants (front-end until fully CMS-owned).
 */

/**
 * Web-optimised home hero video (H.264 MP4 in `public/home/hero.mp4`).
 * Prefer this over the Sanity file asset so the site never serves the master.
 */
export const HOME_HERO_VIDEO_PUBLIC_PATH = '/home/hero.mp4' as const

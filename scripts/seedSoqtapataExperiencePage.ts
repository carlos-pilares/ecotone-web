/**
 * @deprecated The previous payload-only seeder. Use the structured CMS seed instead.
 *   npm run seed:cms
 *   (or npm run seed:soqtapata-page, same runner)
 * Kept for backwards compatibility with older docs / CI.
 */
import './runSeedCmsAll'
